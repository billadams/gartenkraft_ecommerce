@if (Session["Cart"] != null)
{
    <a href="@Url.Action("Index", "Cart")" style="z-index:999;color:#444444;font-size:1.5em;"><span class="glyphicon glyphicon-shopping-cart"></span>(@(((Gartenkraft.Models.Cart)Session["Cart"]).CartItems.Count().ToString()))</a>
}
else
{
    <a href="@Url.Action("Index", "Cart")" style="z-index:999;color:#444444;font-size:1.5em;"><span class="glyphicon glyphicon-shopping-cart"></span></a>
}
