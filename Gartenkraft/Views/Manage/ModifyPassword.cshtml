@model Gartenkraft.ViewModels.ModifyPasswordModel

@if (Model.HasPassword)
{
    @*@Ajax.ActionLink("Change your password", "PartialChangePassword", "Manage", new { }, new AjaxOptions() { HttpMethod = "GET", UpdateTargetId = "ManageContainer", InsertionMode = InsertionMode.ReplaceWith }, htmlAttributes: new { @class = "btn btn-primary" })*@
    @Html.ActionLink("Change your password", "ChangePassword", null, htmlAttributes: new { @class = "btn btn-default navbar-btn" })
}
else
{
    @Html.ActionLink("Create a password", "SetPassword", null, htmlAttributes: new { @class = "btn btn-default navbar-btn" })
}